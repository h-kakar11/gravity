// Placeholder -- the real Queue screen (live job list, progress, cancel/pause/resume/retry,
// copy-path, ETA/speed) lands in Phase 3. This exists now so AppShell's nav has somewhere
// to go rather than a dead link.
export default function QueuePage() {
  return (
    <div style={{ padding: 48, textAlign: "center", color: "var(--color-text-secondary)" }}>
      <h2 style={{ color: "var(--color-text-primary)" }}>Queue</h2>
      <p>Coming soon.</p>
    </div>
  );
}
