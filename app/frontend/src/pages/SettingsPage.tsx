// Placeholder -- the real Settings screen (General/Downloads/Processing/Privacy/Advanced,
// backed by getSettings/updateSettings) lands in Phase 3. This exists now so AppShell's
// nav has somewhere to go rather than a dead link.
export default function SettingsPage() {
  return (
    <div style={{ padding: 48, textAlign: "center", color: "var(--color-text-secondary)" }}>
      <h2 style={{ color: "var(--color-text-primary)" }}>Settings</h2>
      <p>Coming soon.</p>
    </div>
  );
}
