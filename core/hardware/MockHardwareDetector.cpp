#include "core/hardware/MockHardwareDetector.h"

// Fully header-only (a single field getter) -- this translation unit exists only so the
// class has an out-of-line home matching every other component's .h/.cpp pairing.
